---
phase: 06-delivery-and-user-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/qa-start.md
  - .claude/commands/qa-analyze.md
  - .claude/commands/qa-validate.md
  - .claude/commands/qa-testid.md
  - .claude/commands/qa-fix.md
  - .claude/commands/qa-pom.md
  - .claude/commands/qa-audit.md
  - .claude/commands/qa-gap.md
  - .claude/commands/qa-blueprint.md
  - .claude/commands/qa-report.md
  - .claude/commands/qa-pyramid.md
  - .claude/commands/create-test.md
  - .claude/commands/update-test.md
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04]

must_haves:
  truths:
    - "/qa-start invokes the pipeline orchestrator via Task() with user arguments"
    - "/qa-analyze invokes scanner + analyzer agents directly without generation or PR"
    - "/qa-validate invokes validator + bug-detective agents directly"
    - "Each of the 10 Tier 3 commands invokes its correct agent(s) via Task()"
    - "All 13 commands are thin wrappers that delegate to agents, not implementation"
  artifacts:
    - path: ".claude/commands/qa-start.md"
      provides: "Tier 1 primary command -- full pipeline"
      contains: "qa-pipeline-orchestrator.md"
    - path: ".claude/commands/qa-analyze.md"
      provides: "Tier 2 analysis-only command"
      contains: "qaa-scanner.md"
    - path: ".claude/commands/qa-validate.md"
      provides: "Tier 2 validation command"
      contains: "qaa-validator.md"
    - path: ".claude/commands/qa-testid.md"
      provides: "Tier 2 testid injection command"
      contains: "qaa-testid-injector.md"
    - path: ".claude/commands/qa-fix.md"
      provides: "Tier 3 bug fix command"
      contains: "qaa-bug-detective.md"
    - path: ".claude/commands/qa-pom.md"
      provides: "Tier 3 POM generation command"
      contains: "qaa-executor.md"
    - path: ".claude/commands/qa-audit.md"
      provides: "Tier 3 audit command"
      contains: "qaa-validator.md"
    - path: ".claude/commands/qa-gap.md"
      provides: "Tier 3 gap analysis command"
      contains: "qaa-analyzer.md"
    - path: ".claude/commands/qa-blueprint.md"
      provides: "Tier 3 blueprint command"
      contains: "qaa-analyzer.md"
    - path: ".claude/commands/qa-report.md"
      provides: "Tier 3 status report command"
      contains: "qaa-analyzer.md"
    - path: ".claude/commands/qa-pyramid.md"
      provides: "Tier 3 pyramid analysis command"
      contains: "qaa-analyzer.md"
    - path: ".claude/commands/create-test.md"
      provides: "Tier 3 test creation command"
      contains: "qaa-executor.md"
    - path: ".claude/commands/update-test.md"
      provides: "Tier 3 test update command"
      contains: "qaa-validator.md"
  key_links:
    - from: ".claude/commands/qa-start.md"
      to: "agents/qa-pipeline-orchestrator.md"
      via: "Task() with execution_context"
      pattern: "@agents/qa-pipeline-orchestrator.md"
    - from: ".claude/commands/qa-analyze.md"
      to: "agents/qaa-scanner.md"
      via: "Task() with execution_context"
      pattern: "@agents/qaa-scanner.md"
    - from: ".claude/commands/qa-validate.md"
      to: "agents/qaa-validator.md"
      via: "Task() with execution_context"
      pattern: "@agents/qaa-validator.md"
---

<objective>
Rewrite all 13 slash commands to invoke the real agent pipeline instead of containing generic manual instructions. Each command becomes a thin wrapper that gathers user input and delegates to the correct agent(s) via Task().

Purpose: Without working slash commands, QA engineers cannot invoke the system. These are the user's entry points.
Output: 13 rewritten `.claude/commands/*.md` files organized in 3 tiers.
</objective>

<execution_context>
@C:/Users/mrrai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mrrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-delivery-and-user-experience/06-CONTEXT.md
@.planning/phases/06-delivery-and-user-experience/06-RESEARCH.md

<interfaces>
<!-- Agent files that commands must reference -->

Available agents (in agents/ directory):
- qa-pipeline-orchestrator.md -- Full pipeline orchestrator, invoked by /qa-start
- qaa-scanner.md -- Repo scanning, invoked in analysis commands
- qaa-analyzer.md -- Analysis and inventory, invoked by /qa-analyze, /qa-gap, /qa-blueprint, /qa-report, /qa-pyramid
- qaa-executor.md -- Test file generation, invoked by /create-test, /qa-pom
- qaa-validator.md -- 4-layer validation (validation mode) or scoring (audit mode), invoked by /qa-validate, /qa-audit, /update-test
- qaa-testid-injector.md -- Test ID injection, invoked by /qa-testid
- qaa-bug-detective.md -- Failure classification and auto-fix, invoked by /qa-fix
- qaa-planner.md -- Generation plan creation (internal to pipeline, not directly invoked by commands)

Slash command pattern (from RESEARCH.md):
```markdown
# Command Title

Description of what the command does.

## Usage

/command-name [arguments]

## Instructions

1. Read CLAUDE.md
2. [Initialize context if needed]
3. Invoke agent via Task():

Task(
  prompt="
    <objective>What to do</objective>
    <execution_context>@agents/agent-file.md</execution_context>
    <files_to_read>
    - CLAUDE.md
    </files_to_read>
    <parameters>
    user_input: $ARGUMENTS
    </parameters>
  "
)

$ARGUMENTS
```

Key routing rules from RESEARCH.md analysis:
- /qa-start -> orchestrator (full pipeline)
- /qa-analyze -> scanner + analyzer (no generation, no PR)
- /qa-validate -> validator + bug-detective (no scanning, no generation)
- /qa-testid -> scanner + testid-injector (modifies source code)
- /qa-fix -> bug-detective only (never touches app code)
- /qa-pom -> executor in POM-only mode
- /qa-audit -> validator in audit mode (QA_AUDIT_REPORT.md)
- /qa-gap -> scanner (both repos) + analyzer in gap mode (GAP_ANALYSIS.md)
- /qa-blueprint -> scanner + analyzer in full mode (QA_REPO_BLUEPRINT.md)
- /qa-report -> analyzer for status report
- /qa-pyramid -> analyzer for pyramid analysis
- /create-test -> executor for specific feature
- /update-test -> validator audit + executor improvements
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Tier 1 and Tier 2 commands (qa-start, qa-analyze, qa-validate, qa-testid)</name>
  <files>.claude/commands/qa-start.md, .claude/commands/qa-analyze.md, .claude/commands/qa-validate.md, .claude/commands/qa-testid.md</files>
  <read_first>
    - .claude/commands/qa-start.md (current content to understand structure)
    - .claude/commands/qa-analyze.md (current content)
    - .claude/commands/qa-validate.md (current content)
    - .claude/commands/qa-testid.md (current content)
    - agents/qa-pipeline-orchestrator.md (lines 1-15, purpose section -- understand invocation interface)
    - agents/qaa-scanner.md (first 20 lines -- understand purpose and required_reading)
    - agents/qaa-analyzer.md (first 20 lines)
    - agents/qaa-validator.md (first 20 lines)
    - agents/qaa-testid-injector.md (first 20 lines)
    - agents/qaa-bug-detective.md (first 20 lines)
    - .planning/phases/06-delivery-and-user-experience/06-RESEARCH.md (Slash Command Pattern sections, lines 429-499)
    - CLAUDE.md (Agent Pipeline section for stage ordering)
  </read_first>
  <action>
Rewrite each of the 4 Tier 1/2 commands completely. Each command must be a THIN WRAPPER (under 80 lines per locked decision from RESEARCH pitfall 3). Commands gather user input and delegate to agents -- they do NOT contain implementation logic.

**CRITICAL:** All commands must end with `$ARGUMENTS` on the last line (this is how Claude Code passes user arguments to the command).

---

**1. `.claude/commands/qa-start.md` (Tier 1 -- THE primary command)**

Structure:
```markdown
# QA Automation -- Full Pipeline

Run the complete QA automation pipeline. Analyzes a repository, generates a standards-compliant test suite, validates it, and delivers everything as a draft PR.

## Usage

/qa-start [--dev-repo <path>] [--qa-repo <path>] [--auto]

- No arguments: uses current directory as dev repo (Option 1)
- --dev-repo: explicit path to developer repository
- --qa-repo: path to existing QA repository (triggers Option 2 or 3)
- --auto: enable auto-advance mode (no pauses at safe checkpoints)

## Instructions

1. Read `CLAUDE.md` -- all QA standards that govern the pipeline.
2. Read `agents/qa-pipeline-orchestrator.md` -- the pipeline controller.
3. Invoke the orchestrator:

Task(
  prompt="
    <objective>Run complete QA automation pipeline</objective>
    <execution_context>@agents/qa-pipeline-orchestrator.md</execution_context>
    <files_to_read>
    - CLAUDE.md
    </files_to_read>
    <parameters>
    user_input: $ARGUMENTS
    </parameters>
  "
)

$ARGUMENTS
```

---

**2. `.claude/commands/qa-analyze.md` (Tier 2 -- analysis only)**

Structure:
```markdown
# QA Repository Analysis

Analysis-only mode. Scans a repository, detects framework and stack, and produces QA assessment documents. No test generation. No PR creation.

## Usage

/qa-analyze [--dev-repo <path>] [--qa-repo <path>]

- No arguments: analyzes current directory
- --dev-repo: explicit path to developer repository
- --qa-repo: path to existing QA repository (produces gap analysis instead of blueprint)

## What It Produces

- SCAN_MANIFEST.md -- file tree, framework detection, testable surfaces
- QA_ANALYSIS.md -- architecture overview, risk assessment, testing pyramid
- TEST_INVENTORY.md -- prioritized test cases with IDs and explicit outcomes
- QA_REPO_BLUEPRINT.md (if no QA repo) or GAP_ANALYSIS.md (if QA repo provided)

## Instructions

1. Read `CLAUDE.md` -- all QA standards.
2. Initialize pipeline context:
   ```bash
   node bin/qaa-tools.cjs init qa-start [user arguments]
   ```
3. Invoke scanner agent:

Task(
  prompt="
    <objective>Scan repository and produce SCAN_MANIFEST.md</objective>
    <execution_context>@agents/qaa-scanner.md</execution_context>
    <files_to_read>
    - CLAUDE.md
    </files_to_read>
    <parameters>
    user_input: $ARGUMENTS
    </parameters>
  "
)

4. Invoke analyzer agent:

Task(
  prompt="
    <objective>Analyze repository and produce QA_ANALYSIS.md, TEST_INVENTORY.md, and blueprint or gap analysis</objective>
    <execution_context>@agents/qaa-analyzer.md</execution_context>
    <files_to_read>
    - CLAUDE.md
    - .qa-output/SCAN_MANIFEST.md
    </files_to_read>
    <parameters>
    user_input: $ARGUMENTS
    </parameters>
  "
)

5. Present results to user. No git operations. No test generation.

$ARGUMENTS
```

---

**3. `.claude/commands/qa-validate.md` (Tier 2 -- validation + classification)**

Structure:
```markdown
# QA Test Validation

Validate existing test files against CLAUDE.md standards. Runs 4-layer validation (syntax, structure, dependencies, logic) and classifies any failures found.

## Usage

/qa-validate <path-to-tests> [--framework <name>]

- path-to-tests: directory or specific test files to validate
- --framework: override framework auto-detection (playwright, cypress, jest, etc.)

## What It Produces

- VALIDATION_REPORT.md -- pass/fail per file per validation layer, confidence level
- FAILURE_CLASSIFICATION_REPORT.md -- if failures found, classifies as APP BUG / TEST ERROR / ENV ISSUE / INCONCLUSIVE

## Instructions

1. Read `CLAUDE.md` -- quality gates, locator tiers, assertion rules.
2. Invoke validator agent:

Task(
  prompt="
    <objective>Validate test files with 4-layer validation and produce VALIDATION_REPORT.md</objective>
    <execution_context>@agents/qaa-validator.md</execution_context>
    <files_to_read>
    - CLAUDE.md
    </files_to_read>
    <parameters>
    user_input: $ARGUMENTS
    mode: validation
    </parameters>
  "
)

3. If failures detected, invoke bug-detective agent:

Task(
  prompt="
    <objective>Classify test failures and auto-fix TEST CODE ERRORS</objective>
    <execution_context>@agents/qaa-bug-detective.md</execution_context>
    <files_to_read>
    - CLAUDE.md
    - .qa-output/VALIDATION_REPORT.md
    </files_to_read>
    <parameters>
    user_input: $ARGUMENTS
    </parameters>
  "
)

4. Present results. No git operations.

$ARGUMENTS
```

---

**4. `.claude/commands/qa-testid.md` (Tier 2 -- test ID injection)**

Structure:
```markdown
# Inject Test IDs

Scan frontend source code, audit missing data-testid attributes, and inject them following the naming convention in CLAUDE.md. Creates a separate branch for the changes.

## Usage

/qa-testid <path-to-frontend-source>

- path-to-frontend-source: directory containing React/Vue/Angular/HTML components

## What It Produces

- TESTID_AUDIT_REPORT.md -- coverage score, missing elements, proposed values by priority
- Modified source files with data-testid attributes injected

## Instructions

1. Read `CLAUDE.md` -- data-testid Convention section for naming rules.
2. Initialize context:
   ```bash
   node bin/qaa-tools.cjs init qa-start --dev-repo [user path]
   ```
3. Invoke scanner to identify component files:

Task(
  prompt="
    <objective>Scan repository to identify frontend component files</objective>
    <execution_context>@agents/qaa-scanner.md</execution_context>
    <files_to_read>
    - CLAUDE.md
    </files_to_read>
    <parameters>
    user_input: $ARGUMENTS
    </parameters>
  "
)

4. Invoke testid-injector agent:

Task(
  prompt="
    <objective>Audit missing data-testid attributes and inject following naming convention</objective>
    <execution_context>@agents/qaa-testid-injector.md</execution_context>
    <files_to_read>
    - CLAUDE.md
    - .qa-output/SCAN_MANIFEST.md
    </files_to_read>
    <parameters>
    user_input: $ARGUMENTS
    </parameters>
  "
)

$ARGUMENTS
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const cmds={start:['qa-pipeline-orchestrator.md','ARGUMENTS'],analyze:['qaa-scanner.md','qaa-analyzer.md','ARGUMENTS'],validate:['qaa-validator.md','qaa-bug-detective.md','ARGUMENTS'],testid:['qaa-testid-injector.md','ARGUMENTS']}; let fail=false; for(const[name,checks] of Object.entries(cmds)){const f=fs.readFileSync('.claude/commands/qa-'+name+'.md','utf8'); for(const c of checks){if(!f.includes(c)){console.error('qa-'+name+'.md missing: '+c);fail=true;}}} if(fail)process.exit(1); console.log('All 4 Tier 1/2 commands reference correct agents');"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/qa-start.md references "qa-pipeline-orchestrator.md" and ends with "$ARGUMENTS"
    - .claude/commands/qa-analyze.md references "qaa-scanner.md" and "qaa-analyzer.md" and ends with "$ARGUMENTS"
    - .claude/commands/qa-validate.md references "qaa-validator.md" and "qaa-bug-detective.md" and ends with "$ARGUMENTS"
    - .claude/commands/qa-testid.md references "qaa-testid-injector.md" and ends with "$ARGUMENTS"
    - None of the 4 files references old "skill" terminology
    - Each file is under 80 lines
  </acceptance_criteria>
  <done>All 4 Tier 1/2 slash commands rewritten as thin wrappers invoking correct agents via Task() with $ARGUMENTS pass-through.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite all 9 Tier 3 commands (qa-fix through update-test)</name>
  <files>.claude/commands/qa-fix.md, .claude/commands/qa-pom.md, .claude/commands/qa-audit.md, .claude/commands/qa-gap.md, .claude/commands/qa-blueprint.md, .claude/commands/qa-report.md, .claude/commands/qa-pyramid.md, .claude/commands/create-test.md, .claude/commands/update-test.md</files>
  <read_first>
    - .claude/commands/qa-fix.md (current content)
    - .claude/commands/qa-pom.md (current content)
    - .claude/commands/qa-audit.md (current content)
    - .claude/commands/qa-gap.md (current content)
    - .claude/commands/qa-blueprint.md (current content)
    - .claude/commands/qa-report.md (current content)
    - .claude/commands/qa-pyramid.md (current content)
    - .claude/commands/create-test.md (current content)
    - .claude/commands/update-test.md (current content)
    - .planning/phases/06-delivery-and-user-experience/06-RESEARCH.md (Tier 3 command analysis, lines 209-253)
    - CLAUDE.md (Module Boundaries table -- which agent produces which artifact)
  </read_first>
  <action>
Rewrite each of the 9 Tier 3 commands. Same pattern as Tier 1/2: thin wrappers under 80 lines, delegate to agents via Task(), end with `$ARGUMENTS`.

**CRITICAL ROUTING (from RESEARCH.md and CLAUDE.md Module Boundaries):**

| Command | Agent(s) | Mode/Notes |
|---------|----------|------------|
| /qa-fix | qaa-bug-detective.md | Never touches app code, only test code |
| /qa-pom | qaa-executor.md | POM-only mode, checks BasePage first |
| /qa-audit | qaa-validator.md | Audit mode (produces QA_AUDIT_REPORT.md, not VALIDATION_REPORT.md) |
| /qa-gap | qaa-scanner.md + qaa-analyzer.md | Requires 2 repo paths, produces GAP_ANALYSIS.md |
| /qa-blueprint | qaa-scanner.md + qaa-analyzer.md | Dev-only, produces QA_REPO_BLUEPRINT.md |
| /qa-report | qaa-analyzer.md | Status report from existing artifacts or fresh scan |
| /qa-pyramid | qaa-analyzer.md | Pyramid analysis, compares actual vs target |
| /create-test | qaa-executor.md | Feature-focused test creation, reads existing analysis if available |
| /update-test | qaa-validator.md (audit) + qaa-executor.md | Audit first, then improve. Never delete working tests. |

**For each command, follow this template:**

```markdown
# [Command Title]

[One-sentence description of what it does.]

## Usage

/command-name [arguments with descriptions]

## What It Produces

- [Artifact 1] -- [description]

## Instructions

1. Read `CLAUDE.md` -- [specific sections relevant to this command].
2. [Optional init step if context is needed]
3. Invoke [agent name]:

Task(
  prompt="
    <objective>[What to accomplish]</objective>
    <execution_context>@agents/[agent-file].md</execution_context>
    <files_to_read>
    - CLAUDE.md
    [- additional inputs if needed]
    </files_to_read>
    <parameters>
    user_input: $ARGUMENTS
    [mode: specific_mode if applicable]
    </parameters>
  "
)

[4. Optional second agent invocation if command chains agents]

$ARGUMENTS
```

**Specific content for each command:**

**qa-fix.md:** Title "Fix Broken Tests". Invokes bug-detective. Usage: `/qa-fix <path-to-tests> [error output]`. Produces FAILURE_CLASSIFICATION_REPORT.md. Key note: "Never touches application code -- only reports APP BUGs for developer attention."

**qa-pom.md:** Title "Generate Page Object Models". Invokes executor in POM-only mode. Usage: `/qa-pom <path-to-pages> [--framework <name>]`. Produces POM files. References CLAUDE.md POM rules.

**qa-audit.md:** Title "Full QA Audit". Invokes validator in audit mode. Usage: `/qa-audit <path-to-tests> [--dev-repo <path>]`. Produces QA_AUDIT_REPORT.md with 6-dimension scoring. Mode parameter: `mode: audit`.

**qa-gap.md:** Title "QA Gap Analysis". Invokes scanner (both repos) then analyzer in gap mode. Usage: `/qa-gap --dev-repo <path> --qa-repo <path>`. Produces GAP_ANALYSIS.md. Requires 2 repo paths.

**qa-blueprint.md:** Title "QA Repository Blueprint". Invokes scanner then analyzer in full mode. Usage: `/qa-blueprint [--dev-repo <path>]`. Produces QA_REPO_BLUEPRINT.md. Dev-only (no QA repo).

**qa-report.md:** Title "QA Status Report". Invokes analyzer for status reporting. Usage: `/qa-report <path-to-tests> [--dev-repo <path>] [--audience <team|management|client>]`. Produces QA_STATUS_REPORT.md.

**qa-pyramid.md:** Title "Testing Pyramid Analysis". Invokes analyzer for pyramid analysis. Usage: `/qa-pyramid <path-to-tests> [--dev-repo <path>]`. Produces PYRAMID_ANALYSIS.md.

**create-test.md:** Title "Create Automated Tests". Invokes executor for specific feature. Usage: `/create-test <feature-name> [--dev-repo <path>]`. Produces test files + POMs. Reads existing QA_ANALYSIS.md and TEST_INVENTORY.md if available.

**update-test.md:** Title "Update and Improve Existing Tests". Invokes validator in audit mode first, then executor for improvements. Usage: `/update-test <path-to-tests> [--scope fix|improve|add|full]`. Key rule: "NEVER delete or rewrite working tests without user approval."
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const map={'qa-fix':'qaa-bug-detective.md','qa-pom':'qaa-executor.md','qa-audit':'qaa-validator.md','qa-gap':'qaa-analyzer.md','qa-blueprint':'qaa-analyzer.md','qa-report':'qaa-analyzer.md','qa-pyramid':'qaa-analyzer.md','create-test':'qaa-executor.md','update-test':'qaa-validator.md'}; let fail=false; for(const[cmd,agent] of Object.entries(map)){const f=fs.readFileSync('.claude/commands/'+cmd+'.md','utf8'); if(!f.includes(agent)){console.error(cmd+'.md missing agent ref: '+agent);fail=true;} if(!f.includes('ARGUMENTS')){console.error(cmd+'.md missing ARGUMENTS');fail=true;} if(f.includes('skill')){console.error(cmd+'.md still references old skill terminology');fail=true;}} if(fail)process.exit(1); console.log('All 9 Tier 3 commands reference correct agents, have ARGUMENTS, no skill refs');"</automated>
  </verify>
  <acceptance_criteria>
    - qa-fix.md references "qaa-bug-detective.md"
    - qa-pom.md references "qaa-executor.md"
    - qa-audit.md references "qaa-validator.md" with mode "audit"
    - qa-gap.md references "qaa-scanner.md" and "qaa-analyzer.md"
    - qa-blueprint.md references "qaa-scanner.md" and "qaa-analyzer.md"
    - qa-report.md references "qaa-analyzer.md"
    - qa-pyramid.md references "qaa-analyzer.md"
    - create-test.md references "qaa-executor.md"
    - update-test.md references "qaa-validator.md" and "qaa-executor.md"
    - All 9 files end with "$ARGUMENTS"
    - None contain the word "skill" (old terminology)
    - Each file is under 80 lines
  </acceptance_criteria>
  <done>All 9 Tier 3 slash commands rewritten as thin wrappers invoking correct agents. No old "skill" references remain. Every command under 80 lines.</done>
</task>

</tasks>

<verification>
1. All 13 commands in .claude/commands/ reference real agent files (not "skill" terminology)
2. Tier 1 (qa-start) references qa-pipeline-orchestrator.md
3. Tier 2 commands reference scanner, analyzer, validator, bug-detective, testid-injector agents
4. Tier 3 commands route to correct agents per Module Boundaries table
5. All 13 commands end with $ARGUMENTS
6. No command exceeds 80 lines
7. No command contains implementation logic that duplicates agent behavior
</verification>

<success_criteria>
- All 13 slash commands rewritten as thin wrappers invoking the real agent pipeline
- /qa-start invokes orchestrator (UX-01)
- /qa-analyze invokes scanner + analyzer (UX-02)
- /qa-validate invokes validator + bug-detective (UX-03)
- 10 Tier 3 commands invoke correct focused agents (UX-04)
- No "skill" terminology remains in any command
</success_criteria>

<output>
After completion, create `.planning/phases/06-delivery-and-user-experience/06-02-SUMMARY.md`
</output>
