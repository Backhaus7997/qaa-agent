# Phase 6: Delivery and User Experience - Context

**Gathered:** 2026-03-19
**Status:** Ready for planning

<domain>
## Phase Boundary

Wire up the deliver stage in the orchestrator (branch creation, atomic commits, PR via gh CLI), rewrite all 13 slash commands to use the real agent pipeline, create the PR template, and write a junior-friendly README. This completes the system — after this phase, any QA engineer can run /qa-start and get a PR.

</domain>

<decisions>
## Implementation Decisions

### PR Content & Format
- Full summary PR description (50-100 lines): analysis highlights, test counts by pyramid level, coverage metrics, validation pass/fail, list of generated files
- Reviewer checklist included: [ ] Test IDs unique, [ ] Assertions concrete, [ ] POM no assertions, [ ] No hardcoded credentials, etc.
- Labels: 'qa-automation', 'auto-generated'. Assigned to the QA engineer who ran the agent.
- Created as draft PR — QA engineer reviews artifacts, marks ready when satisfied
- Targets main/master (default branch)
- All PR content in English

### Slash Command Updates
- Rewrite ALL 13 commands to reference the real pipeline (orchestrator + agents)
- /qa-start is THE primary command — full pipeline, one-command experience
- Commands organized in 3 tiers:
  - Tier 1 (daily): /qa-start
  - Tier 2 (common): /qa-analyze, /qa-validate, /qa-testid
  - Tier 3 (specialized): /qa-fix, /qa-pom, /qa-audit, /qa-gap, /qa-blueprint, /qa-report, /qa-pyramid, /create-test, /update-test

### README
- Junior-friendly: step-by-step setup, detailed prerequisites, troubleshooting section
- Includes full example terminal output showing a successful /qa-start run (user input → agent banners → PR link)
- Quick-start section for seniors + detailed walkthrough for juniors

### Claude's Discretion
- Exact PR template markdown structure
- README section ordering
- How secondary commands reference agents internally
- Troubleshooting section content

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Existing Commands (to be rewritten)
- `.claude/commands/qa-start.md` through all 13 command files — current versions need updating
- `.claude/commands/` — full directory listing

### Orchestrator (commands invoke this)
- `agents/qa-pipeline-orchestrator.md` — Main pipeline (1,026 lines, 3 options)

### Infrastructure
- `bin/qaa-tools.cjs` — CLI with commit command for PR creation
- `bin/lib/commands.cjs` — cmdCommit for atomic commits
- `CLAUDE.md` — Git workflow section (branch naming, commit format)

### Existing PR Template
- `templates/` — Check if pr-template.md exists from project spec

### Standards
- `CLAUDE.md` — Module boundaries, verification commands, agent coordination, quality gates (reviewer checklist source)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- 13 slash commands already exist in `.claude/commands/` — structure is correct, content needs updating
- `agents/qa-pipeline-orchestrator.md` has a stubbed deliver stage ready to be completed
- `CLAUDE.md` already has Git Workflow section with branch naming and commit format
- `bin/lib/commands.cjs` has cmdCommit for atomic staging

### Established Patterns
- Slash commands are markdown files in `.claude/commands/` — Claude Code reads them when user types `/command-name`
- GSD's PR pattern: `gh pr create --title "..." --body "..."` with HEREDOC for body
- Orchestrator sets stage to 'deliver' → creates branch → commits → pushes → creates PR

### Integration Points
- /qa-start invokes orchestrator → orchestrator runs pipeline → deliver stage creates PR
- /qa-analyze invokes scanner + analyzer directly (no executor/validator/deliver)
- /qa-validate invokes validator + bug-detective directly
- Tier 3 commands invoke individual agents for focused tasks

</code_context>

<specifics>
## Specific Ideas

- PR template should include a "Generated by QAA" badge/footer so the team recognizes auto-generated PRs
- README should list ALL prerequisites explicitly: Node.js 18+, Claude Code, gh CLI authenticated, Pro/Max plan for Opus
- Example output in README should show the full flow including the PR link at the end

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 06-delivery-and-user-experience*
*Context gathered: 2026-03-19*
