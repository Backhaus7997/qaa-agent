---
phase: 01-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/model-profiles.cjs
  - bin/lib/core.cjs
  - bin/lib/frontmatter.cjs
autonomous: true
requirements: [INFRA-02, INFRA-05]

must_haves:
  truths:
    - "model-profiles.cjs exports MODEL_PROFILES with 7 qaa- agent types"
    - "core.cjs exports 21 utility functions with qaa- naming throughout"
    - "frontmatter.cjs parses and serializes YAML frontmatter correctly"
    - "All three modules load without errors via node --check"
  artifacts:
    - path: bin/lib/model-profiles.cjs
      provides: "QAA agent-to-model mapping"
      contains: "qaa-scanner"
      exports: ["MODEL_PROFILES", "VALID_PROFILES", "formatAgentToModelMapAsTable", "getAgentToModelMapForProfile"]
    - path: bin/lib/core.cjs
      provides: "Shared utilities, output protocol, phase math"
      contains: "qaa-"
      exports: ["output", "error", "loadConfig", "resolveModelInternal"]
    - path: bin/lib/frontmatter.cjs
      provides: "YAML frontmatter parsing and CRUD commands"
      contains: "extractFrontmatter"
      exports: ["extractFrontmatter", "reconstructFrontmatter", "spliceFrontmatter"]
  key_links:
    - from: bin/lib/core.cjs
      to: bin/lib/model-profiles.cjs
      via: "require('./model-profiles.cjs')"
      pattern: "require.*model-profiles"
    - from: bin/lib/frontmatter.cjs
      to: bin/lib/core.cjs
      via: "require('./core.cjs')"
      pattern: "require.*core\\.cjs"
---

<objective>
Port the three foundation modules from GSD to QAA: model-profiles.cjs, core.cjs, and frontmatter.cjs. These are leaf/near-leaf dependencies that all other modules require.

Purpose: Every other module in the QAA tooling depends on these three. They must exist and work correctly before any higher-level module can be ported.
Output: Three working .cjs files in bin/lib/ with all gsd->qaa renames applied.
</objective>

<execution_context>
@C:/Users/mrrai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mrrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bin/lib/ directory and port model-profiles.cjs</name>
  <files>bin/lib/model-profiles.cjs</files>
  <read_first>
    C:/Users/mrrai/.claude/get-shit-done/bin/lib/model-profiles.cjs
  </read_first>
  <action>
Create the directory structure `bin/lib/` at the project root.

Port `model-profiles.cjs` (69 lines) from the GSD source at `C:/Users/mrrai/.claude/get-shit-done/bin/lib/model-profiles.cjs`. This is a faithful copy with the following changes:

1. Replace the entire MODEL_PROFILES object (lines 9-25) with 7 QAA agent types:
```javascript
const MODEL_PROFILES = {
  'qaa-scanner':          { quality: 'opus',   balanced: 'sonnet', budget: 'haiku' },
  'qaa-analyzer':         { quality: 'opus',   balanced: 'sonnet', budget: 'haiku' },
  'qaa-planner':          { quality: 'opus',   balanced: 'opus',   budget: 'sonnet' },
  'qaa-executor':         { quality: 'opus',   balanced: 'sonnet', budget: 'sonnet' },
  'qaa-validator':        { quality: 'sonnet', balanced: 'sonnet', budget: 'haiku' },
  'qaa-testid-injector':  { quality: 'sonnet', balanced: 'sonnet', budget: 'haiku' },
  'qaa-bug-detective':    { quality: 'opus',   balanced: 'sonnet', budget: 'sonnet' },
};
```

2. Update the JSDoc comment at the top to reference "QAA agent" instead of "GSD agent".

3. Keep all four exports exactly as-is: `MODEL_PROFILES`, `VALID_PROFILES`, `formatAgentToModelMapAsTable`, `getAgentToModelMapForProfile`.

4. Keep `VALID_PROFILES` derivation: `const VALID_PROFILES = Object.keys(MODEL_PROFILES['qaa-planner']);` (changed from 'gsd-planner').

Do NOT change the function signatures or logic of `formatAgentToModelMapAsTable` or `getAgentToModelMapForProfile` -- they are generic and work with any agent names.
  </action>
  <verify>
    <automated>node --check bin/lib/model-profiles.cjs && node -e "const m = require('./bin/lib/model-profiles.cjs'); console.log(JSON.stringify({keys: Object.keys(m.MODEL_PROFILES), profiles: m.VALID_PROFILES, scanner: m.MODEL_PROFILES['qaa-scanner']}))"</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/model-profiles.cjs exists and passes `node --check`
    - File contains `'qaa-scanner'` as a key in MODEL_PROFILES
    - File contains `'qaa-analyzer'` as a key in MODEL_PROFILES
    - File contains `'qaa-planner'` as a key in MODEL_PROFILES
    - File contains `'qaa-executor'` as a key in MODEL_PROFILES
    - File contains `'qaa-validator'` as a key in MODEL_PROFILES
    - File contains `'qaa-testid-injector'` as a key in MODEL_PROFILES
    - File contains `'qaa-bug-detective'` as a key in MODEL_PROFILES
    - File does NOT contain the string `gsd-` anywhere
    - `module.exports` contains MODEL_PROFILES, VALID_PROFILES, formatAgentToModelMapAsTable, getAgentToModelMapForProfile
    - VALID_PROFILES equals ['quality', 'balanced', 'budget']
    - Running `node -e "require('./bin/lib/model-profiles.cjs')"` exits 0
  </acceptance_criteria>
  <done>model-profiles.cjs exists in bin/lib/ with 7 QAA agent types, zero gsd- references, all 4 exports working</done>
</task>

<task type="auto">
  <name>Task 2: Port core.cjs with qaa- renames</name>
  <files>bin/lib/core.cjs</files>
  <read_first>
    C:/Users/mrrai/.claude/get-shit-done/bin/lib/core.cjs
    bin/lib/model-profiles.cjs
  </read_first>
  <action>
Port `core.cjs` (497 lines) from `C:/Users/mrrai/.claude/get-shit-done/bin/lib/core.cjs`. This is the most import-heavy utility module. Apply these specific renames:

1. **Line 8**: Keep `require('./model-profiles.cjs')` -- path unchanged, it references our new file.

2. **Line 27 (output function, tmpfile prefix)**: Change `gsd-${Date.now()}.json` to `qaa-${Date.now()}.json`.

3. **Line 59 (loadConfig defaults, phase_branch_template)**: Change `'gsd/phase-{phase}-{slug}'` to `'qaa/phase-{phase}-{slug}'`.

4. **Line 60 (loadConfig defaults, milestone_branch_template)**: Change `'gsd/{milestone}-{slug}'` to `'qaa/{milestone}-{slug}'`.

5. **All other code**: Copy verbatim. The 21 exports remain identical:
   `output, error, safeReadFile, loadConfig, isGitIgnored, execGit, escapeRegex, normalizePhaseName, comparePhaseNum, searchPhaseInDir, findPhaseInternal, getArchivedPhaseDirs, getRoadmapPhaseInternal, resolveModelInternal, pathExistsInternal, generateSlugInternal, getMilestoneInfo, getMilestonePhaseFilter, stripShippedMilestones, replaceInCurrentMilestone, toPosixPath`

DO NOT rename any function names, parameter names, or the `.planning` directory path. Only the 3 string literal values listed above change from gsd to qaa.

IMPORTANT: The `resolveModelInternal` function (lines 371-386) works generically via `MODEL_PROFILES[agentType]` lookup, so it automatically supports qaa- agent types without code changes.
  </action>
  <verify>
    <automated>node --check bin/lib/core.cjs && node -e "const c = require('./bin/lib/core.cjs'); console.log(typeof c.output, typeof c.loadConfig, typeof c.resolveModelInternal, typeof c.toPosixPath)"</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/core.cjs exists and passes `node --check`
    - File contains `qaa-${Date.now()}` (tmpfile prefix)
    - File contains `'qaa/phase-{phase}-{slug}'` (branch template default)
    - File contains `'qaa/{milestone}-{slug}'` (milestone branch template default)
    - File does NOT contain the string `'gsd/` or `'gsd-` in any string literal (comments about GSD origin are acceptable)
    - File contains `require('./model-profiles.cjs')`
    - File exports exactly 21 functions matching the GSD source export list
    - Running `node -e "require('./bin/lib/core.cjs')"` exits 0
  </acceptance_criteria>
  <done>core.cjs exists with all 21 exports, qaa- naming in branch templates and tmpfile prefix, loads without error</done>
</task>

<task type="auto">
  <name>Task 3: Port frontmatter.cjs</name>
  <files>bin/lib/frontmatter.cjs</files>
  <read_first>
    C:/Users/mrrai/.claude/get-shit-done/bin/lib/frontmatter.cjs
    bin/lib/core.cjs
  </read_first>
  <action>
Port `frontmatter.cjs` (300 lines) from `C:/Users/mrrai/.claude/get-shit-done/bin/lib/frontmatter.cjs`. This module has NO gsd-specific strings -- it is a generic YAML frontmatter parser.

Copy the entire file verbatim. The only line that references another module is:
```javascript
const { safeReadFile, output, error } = require('./core.cjs');
```
This path is correct as-is since both files live in `bin/lib/`.

The file exports 9 items:
- `extractFrontmatter` - parses YAML frontmatter from markdown content
- `reconstructFrontmatter` - serializes object back to YAML string
- `spliceFrontmatter` - replaces frontmatter in markdown content
- `parseMustHavesBlock` - extracts nested must_haves blocks
- `FRONTMATTER_SCHEMAS` - schema definitions for plan/summary/verification
- `cmdFrontmatterGet` - CLI command: get frontmatter field
- `cmdFrontmatterSet` - CLI command: set frontmatter field
- `cmdFrontmatterMerge` - CLI command: merge JSON into frontmatter
- `cmdFrontmatterValidate` - CLI command: validate against schema

This is a direct copy with zero renames needed. The FRONTMATTER_SCHEMAS object defines required fields for plan, summary, and verification schemas -- keep these exactly as-is.
  </action>
  <verify>
    <automated>node --check bin/lib/frontmatter.cjs && node -e "const f = require('./bin/lib/frontmatter.cjs'); const fm = f.extractFrontmatter('---\nphase: 01\nplan: 01\n---\n# Test'); console.log(JSON.stringify(fm))"</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/frontmatter.cjs exists and passes `node --check`
    - File contains `function extractFrontmatter(`
    - File contains `function reconstructFrontmatter(`
    - File contains `function spliceFrontmatter(`
    - File contains `function parseMustHavesBlock(`
    - File contains `FRONTMATTER_SCHEMAS`
    - File contains `require('./core.cjs')`
    - Running `node -e "const f = require('./bin/lib/frontmatter.cjs'); console.log(Object.keys(f).length)"` outputs 9
    - Running `node -e "const f = require('./bin/lib/frontmatter.cjs'); const r = f.extractFrontmatter('---\\nkey: value\\n---\\n'); console.log(r.key)"` outputs "value"
  </acceptance_criteria>
  <done>frontmatter.cjs exists with all 9 exports, parses YAML frontmatter correctly, loads without error</done>
</task>

</tasks>

<verification>
All three foundation modules load independently and their cross-module requires resolve:
```bash
node --check bin/lib/model-profiles.cjs
node --check bin/lib/core.cjs
node --check bin/lib/frontmatter.cjs
node -e "require('./bin/lib/model-profiles.cjs'); require('./bin/lib/core.cjs'); require('./bin/lib/frontmatter.cjs'); console.log('All foundation modules load OK')"
```
</verification>

<success_criteria>
- Three .cjs files exist in bin/lib/
- All pass node --check (no syntax errors)
- model-profiles.cjs has 7 qaa- agent types, zero gsd- references
- core.cjs has qaa/ branch templates, qaa- tmpfile prefix
- frontmatter.cjs parses and reconstructs YAML frontmatter
- Cross-module requires resolve (core imports model-profiles, frontmatter imports core)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-01-SUMMARY.md`
</output>
