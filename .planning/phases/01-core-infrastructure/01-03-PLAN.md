---
phase: 01-core-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - bin/lib/roadmap.cjs
  - bin/lib/template.cjs
  - bin/lib/milestone.cjs
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "roadmap.cjs parses ROADMAP.md phase sections and updates plan progress"
    - "template.cjs fills summary, plan, and verification templates"
    - "milestone.cjs marks requirements complete and archives milestones"
    - "All three modules load without errors"
  artifacts:
    - path: bin/lib/roadmap.cjs
      provides: "ROADMAP.md parsing and update operations"
      contains: "cmdRoadmapGetPhase"
      exports: ["cmdRoadmapGetPhase", "cmdRoadmapAnalyze", "cmdRoadmapUpdatePlanProgress"]
    - path: bin/lib/template.cjs
      provides: "Template selection and fill operations"
      contains: "cmdTemplateFill"
      exports: ["cmdTemplateSelect", "cmdTemplateFill"]
    - path: bin/lib/milestone.cjs
      provides: "Milestone and requirements lifecycle"
      contains: "cmdMilestoneComplete"
      exports: ["cmdRequirementsMarkComplete", "cmdMilestoneComplete"]
  key_links:
    - from: bin/lib/roadmap.cjs
      to: bin/lib/core.cjs
      via: "require('./core.cjs')"
      pattern: "require.*core\\.cjs"
    - from: bin/lib/template.cjs
      to: bin/lib/frontmatter.cjs
      via: "require('./frontmatter.cjs')"
      pattern: "require.*frontmatter"
    - from: bin/lib/milestone.cjs
      to: bin/lib/state.cjs
      via: "require('./state.cjs')"
      pattern: "require.*state\\.cjs"
---

<objective>
Port three mid-tier modules from GSD to QAA: roadmap.cjs, template.cjs, and milestone.cjs. These modules handle roadmap parsing, template generation, and milestone lifecycle.

Purpose: These modules are required by the CLI router and init system. They provide the roadmap analysis, template filling, and milestone archiving capabilities.
Output: Three working .cjs files in bin/lib/ with appropriate gsd->qaa renames.
</objective>

<execution_context>
@C:/Users/mrrai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mrrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port roadmap.cjs</name>
  <files>bin/lib/roadmap.cjs</files>
  <read_first>
    C:/Users/mrrai/.claude/get-shit-done/bin/lib/roadmap.cjs
    bin/lib/core.cjs
  </read_first>
  <action>
Port `roadmap.cjs` (307 lines) from `C:/Users/mrrai/.claude/get-shit-done/bin/lib/roadmap.cjs`.

This file has ZERO gsd-specific strings. It is entirely generic -- it parses ROADMAP.md by looking for `### Phase N:` headings, extracts goals, dependencies, and success criteria. Copy the entire file verbatim.

The require is:
```javascript
const { escapeRegex, normalizePhaseName, output, error, findPhaseInternal, stripShippedMilestones, replaceInCurrentMilestone } = require('./core.cjs');
```

The file exports 3 functions:
- `cmdRoadmapGetPhase(cwd, phaseNum, raw)` - extracts a phase section from ROADMAP.md
- `cmdRoadmapAnalyze(cwd, raw)` - full roadmap parse with disk status
- `cmdRoadmapUpdatePlanProgress(cwd, phaseNum, raw)` - updates progress table row

Direct copy, no modifications needed.
  </action>
  <verify>
    <automated>node --check bin/lib/roadmap.cjs && node -e "const r = require('./bin/lib/roadmap.cjs'); console.log(typeof r.cmdRoadmapGetPhase, typeof r.cmdRoadmapAnalyze, typeof r.cmdRoadmapUpdatePlanProgress)"</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/roadmap.cjs exists and passes `node --check`
    - File contains `function cmdRoadmapGetPhase(`
    - File contains `function cmdRoadmapAnalyze(`
    - File contains `function cmdRoadmapUpdatePlanProgress(`
    - File contains `require('./core.cjs')`
    - Running `node -e "require('./bin/lib/roadmap.cjs')"` exits 0
  </acceptance_criteria>
  <done>roadmap.cjs exists with 3 exports, loads without error</done>
</task>

<task type="auto">
  <name>Task 2: Port template.cjs with qaa- slash command references</name>
  <files>bin/lib/template.cjs</files>
  <read_first>
    C:/Users/mrrai/.claude/get-shit-done/bin/lib/template.cjs
    bin/lib/core.cjs
    bin/lib/frontmatter.cjs
  </read_first>
  <action>
Port `template.cjs` (223 lines) from `C:/Users/mrrai/.claude/get-shit-done/bin/lib/template.cjs`.

Scan the source for any `/gsd:` slash command references in template body text and rename to `/qa-` per REQUIREMENTS.md (UX-01 through UX-04 use `/qa-start`, `/qa-analyze`, `/qa-validate`). Specifically check the `cmdTemplateFill` function's body string arrays for any `/gsd:plan-phase` or similar references.

The GSD source does NOT contain `/gsd:` references in template.cjs body strings -- the template bodies use generic placeholder text like `[Task name]`, `[What to do]`, etc. So this should be a direct copy.

The requires are:
```javascript
const { normalizePhaseName, findPhaseInternal, generateSlugInternal, toPosixPath, output, error } = require('./core.cjs');
const { reconstructFrontmatter } = require('./frontmatter.cjs');
```

The file exports 2 functions:
- `cmdTemplateSelect(cwd, planPath, raw)` - selects template type based on plan complexity
- `cmdTemplateFill(cwd, templateType, options, raw)` - creates pre-filled PLAN.md, SUMMARY.md, or VERIFICATION.md

Copy verbatim -- no gsd-specific strings to rename.
  </action>
  <verify>
    <automated>node --check bin/lib/template.cjs && node -e "const t = require('./bin/lib/template.cjs'); console.log(typeof t.cmdTemplateSelect, typeof t.cmdTemplateFill)"</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/template.cjs exists and passes `node --check`
    - File contains `function cmdTemplateSelect(`
    - File contains `function cmdTemplateFill(`
    - File contains `require('./core.cjs')`
    - File contains `require('./frontmatter.cjs')`
    - File does NOT contain `/gsd:` in any string literal
    - Running `node -e "require('./bin/lib/template.cjs')"` exits 0
  </acceptance_criteria>
  <done>template.cjs exists with 2 exports, no /gsd: references, loads without error</done>
</task>

<task type="auto">
  <name>Task 3: Port milestone.cjs</name>
  <files>bin/lib/milestone.cjs</files>
  <read_first>
    C:/Users/mrrai/.claude/get-shit-done/bin/lib/milestone.cjs
    bin/lib/core.cjs
    bin/lib/frontmatter.cjs
    bin/lib/state.cjs
  </read_first>
  <action>
Port `milestone.cjs` (242 lines) from `C:/Users/mrrai/.claude/get-shit-done/bin/lib/milestone.cjs`.

This file has ZERO gsd-specific strings. It is entirely generic -- it operates on REQUIREMENTS.md checkboxes and ROADMAP.md archive operations using regex patterns that match requirement IDs and phase numbers. Copy the entire file verbatim.

The requires are:
```javascript
const { escapeRegex, getMilestonePhaseFilter, output, error } = require('./core.cjs');
const { extractFrontmatter } = require('./frontmatter.cjs');
const { writeStateMd } = require('./state.cjs');
```

NOTE: This module depends on state.cjs (via writeStateMd). Plan 01-02 must be complete before this task's verify step can succeed. However, since Plan 01-03 is Wave 2 alongside Plan 01-02, the executor should ensure Plan 01-02 Task 2 (state.cjs) runs first or at minimum that state.cjs exists. If running in parallel with Plan 01-02, the executor should wait for Plan 01-02 to complete.

The file exports 2 functions:
- `cmdRequirementsMarkComplete(cwd, reqIdsRaw, raw)` - marks requirement IDs as complete
- `cmdMilestoneComplete(cwd, version, options, raw)` - archives a milestone

Copy verbatim -- no modifications needed.
  </action>
  <verify>
    <automated>node --check bin/lib/milestone.cjs && node -e "const m = require('./bin/lib/milestone.cjs'); console.log(typeof m.cmdRequirementsMarkComplete, typeof m.cmdMilestoneComplete)"</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/milestone.cjs exists and passes `node --check`
    - File contains `function cmdRequirementsMarkComplete(`
    - File contains `function cmdMilestoneComplete(`
    - File contains `require('./core.cjs')`
    - File contains `require('./frontmatter.cjs')`
    - File contains `require('./state.cjs')`
    - Running `node -e "require('./bin/lib/milestone.cjs')"` exits 0
  </acceptance_criteria>
  <done>milestone.cjs exists with 2 exports, loads without error</done>
</task>

</tasks>

<verification>
All three modules load and cross-module requires resolve:
```bash
node --check bin/lib/roadmap.cjs && node --check bin/lib/template.cjs && node --check bin/lib/milestone.cjs
node -e "require('./bin/lib/roadmap.cjs'); require('./bin/lib/template.cjs'); require('./bin/lib/milestone.cjs'); console.log('All mid-tier modules load OK')"
```
</verification>

<success_criteria>
- Three .cjs files exist in bin/lib/
- All pass node --check
- roadmap.cjs has 3 exports for roadmap parsing
- template.cjs has 2 exports for template operations
- milestone.cjs has 2 exports for milestone lifecycle
- Cross-module requires resolve (all -> core, template -> frontmatter, milestone -> state)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-03-SUMMARY.md`
</output>
