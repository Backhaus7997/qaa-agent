---
phase: 02-qa-standards-and-templates
plan: "04"
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - TMPL-10

must_haves:
  truths:
    - "CLAUDE.md at project root contains ALL existing QA standards preserved verbatim (Framework Detection, Testing Pyramid, Locator Strategy, POM Rules, Test Spec Rules, Naming Conventions, Repo Structure, Test Data Rules, Analysis Documents, Quality Gates)"
    - "CLAUDE.md contains NEW agent pipeline rules with stage transitions and conditions"
    - "CLAUDE.md contains NEW module boundaries table mapping all 7 agents to their artifacts and templates"
    - "CLAUDE.md contains NEW verification commands per artifact type"
    - "CLAUDE.md contains NEW git workflow with branch naming, commit format, and PR template"
    - "CLAUDE.md contains NEW agent coordination rules with read-before-write and handoff patterns"
    - "CLAUDE.md contains NEW data-testid naming convention section"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Complete QA standards and agent coordination rules"
      contains: "Module Boundaries"
  key_links:
    - from: "CLAUDE.md"
      to: "templates/qa-analysis.md"
      via: "Module Boundaries table references template file paths"
      pattern: "templates/qa-analysis.md"
    - from: "CLAUDE.md"
      to: "templates/scan-manifest.md"
      via: "Module Boundaries table references scanner template"
      pattern: "templates/scan-manifest.md"
    - from: "CLAUDE.md"
      to: "templates/validation-report.md"
      via: "Module Boundaries table references validator template"
      pattern: "templates/validation-report.md"
---

<objective>
Create the enhanced CLAUDE.md at project root that serves as the shared brain of the entire QA automation system. This file preserves all existing QA standards and adds agent pipeline rules, module boundaries, verification commands, git workflow, team settings, and agent coordination rules.

Purpose: CLAUDE.md is the single source of truth that all agents read before producing any output. It defines quality standards, artifact ownership, handoff patterns, and verification commands. Without it, agents produce inconsistent, uncoordinated output.

Output: CLAUDE.md at project root with 17 sections covering QA standards + agent coordination, referencing all 9 templates created in plans 01-03.
</objective>

<execution_context>
@C:/Users/mrrai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mrrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-qa-standards-and-templates/02-CONTEXT.md
@.planning/phases/02-qa-standards-and-templates/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enhanced CLAUDE.md at project root (TMPL-10)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - qa-agent-gsd/CLAUDE.md (existing QA standards to preserve verbatim -- all 10 sections)
    - .planning/phases/02-qa-standards-and-templates/02-RESEARCH.md (TMPL-10 section map with Layer 1-7 strategy + code examples for Module Boundaries and Agent Pipeline)
    - .planning/phases/02-qa-standards-and-templates/02-CONTEXT.md (locked decisions: keep ALL existing standards, add agent pipeline, module boundaries, verification commands, git workflow, team settings, agent coordination)
    - .claude/skills/qa-testid-injector/SKILL.md (naming convention for data-testid section)
    - data-testid-SKILL.md (comprehensive naming convention details)
    - templates/scan-manifest.md (verify template exists for Module Boundaries reference)
    - templates/qa-analysis.md (verify template exists for Module Boundaries reference)
    - templates/test-inventory.md (verify template exists for Module Boundaries reference)
  </read_first>
  <action>
Create `CLAUDE.md` at the project root (NOT inside qa-agent-gsd/). This file has 17 sections organized in 7 layers. It will be approximately 450-550 lines.

**LAYER 1: EXISTING QA STANDARDS (preserve verbatim from qa-agent-gsd/CLAUDE.md)**

Copy these 10 sections EXACTLY as they appear in the existing file, preserving all content, code examples, and formatting:

1. **# QA Automation Standards** -- Opening paragraph about strict QA automation standards
2. **## Framework Detection** -- 4-step detection process + "If no framework exists yet, ask the user"
3. **## Testing Pyramid** -- ASCII pyramid with percentages (65/15/15/5 split), "Adjust percentages" note
4. **## Locator Strategy** -- Tier 1-4 hierarchy with Playwright, Cypress, Selenium examples. Include ALL code blocks.
5. **## Page Object Model Rules** -- 6 rules + POM File Structure tree
6. **## Test Spec Rules** -- "Every test case MUST have" 5 fields, BAD assertions examples, GOOD assertions examples
7. **## Naming Conventions** -- Table with patterns for PageObject, Component POM, E2E, API, Unit, Fixture
8. **## Repo Structure** -- Recommended tree with tests/, pages/, fixtures/, config/, reports/
9. **## Test Data Rules** -- 4 bullet points about credentials, env vars, fixtures
10. **## Analysis Documents** -- QA_ANALYSIS.md and TEST_INVENTORY.md requirements
11. **## Quality Gates** -- 9-item checklist

**LAYER 2: AGENT PIPELINE RULES (NEW)**

12. **## Agent Pipeline** -- Add after Quality Gates section.

Content:
- Pipeline stage diagram showing: `scan -> analyze -> [testid-inject if frontend] -> plan -> generate -> validate -> deliver`
- Three workflow options:
  * Option 1 (Dev-only repo): full pipeline
  * Option 2 (Dev + immature QA repo): scan both -> gap analysis -> fix broken -> add missing -> standardize -> validate -> deliver
  * Option 3 (Dev + mature QA repo): scan both -> identify thin coverage -> add only missing -> validate -> deliver
- Stage Transitions table with columns: From, To, Condition. Include all transitions:
  * scan -> analyze: SCAN_MANIFEST.md exists with > 0 testable surfaces
  * analyze -> testid-inject: QA_ANALYSIS.md exists AND frontend components detected
  * analyze -> plan: QA_ANALYSIS.md + TEST_INVENTORY.md exist (skip testid-inject if no frontend)
  * plan -> generate: Generation plan approved (or auto-approved in auto-advance mode)
  * generate -> validate: All planned test files exist on disk
  * validate -> deliver: VALIDATION_REPORT.md shows PASS or max fix loops exhausted

**LAYER 3: MODULE BOUNDARIES (NEW)**

13. **## Module Boundaries** -- Add after Agent Pipeline.

Content:
- File ownership table with columns: Agent, Reads, Produces, Template. Cover ALL 7 agents:
  * qa-scanner: reads repo source files, produces SCAN_MANIFEST.md, template: templates/scan-manifest.md
  * qa-analyzer: reads SCAN_MANIFEST.md, produces QA_ANALYSIS.md + TEST_INVENTORY.md + QA_REPO_BLUEPRINT.md (or GAP_ANALYSIS.md for Option 2/3), templates: templates/qa-analysis.md + templates/test-inventory.md + templates/qa-repo-blueprint.md (or templates/gap-analysis.md)
  * qa-planner: reads TEST_INVENTORY.md + QA_ANALYSIS.md, produces generation plan (internal), template: none
  * qa-executor: reads TEST_INVENTORY.md + CLAUDE.md, produces test files + POMs + fixtures + configs, template: qa-template-engine patterns
  * qa-validator: reads generated test files, produces VALIDATION_REPORT.md (or QA_AUDIT_REPORT.md in audit mode), template: templates/validation-report.md (or templates/qa-audit-report.md)
  * qa-testid-injector: reads repo source files, produces SCAN_MANIFEST.md + TESTID_AUDIT_REPORT.md + modified source files, template: templates/scan-manifest.md + templates/testid-audit-report.md
  * qa-bug-detective: reads test execution results, produces FAILURE_CLASSIFICATION_REPORT.md, template: templates/failure-classification.md

- Rule: "An agent MUST NOT produce artifacts assigned to another agent."
- Rule: "An agent MUST read all artifacts listed in its 'Reads' column before producing output."

**LAYER 4: VERIFICATION COMMANDS (NEW)**

14. **## Verification Commands** -- Add after Module Boundaries.

Content:
- Per artifact type, what command/check validates it:
  * SCAN_MANIFEST.md: Has > 0 files in File List, Project Detection populated, Testable Surfaces has at least 1 category
  * QA_ANALYSIS.md: All 6 sections present, Top 10 has 10 entries, pyramid percentages sum to 100
  * TEST_INVENTORY.md: Every test case has all mandatory fields (ID, target, inputs, expected outcome, priority), IDs are unique, pyramid tier counts match summary
  * QA_REPO_BLUEPRINT.md: Folder structure tree present, config files have actual content (not placeholders), npm scripts defined
  * VALIDATION_REPORT.md: All 4 layers reported per file, confidence level assigned, fix loop log shows iterations
  * FAILURE_CLASSIFICATION_REPORT.md: Every failure has classification + confidence + evidence, no APPLICATION BUG marked as auto-fixed
  * TESTID_AUDIT_REPORT.md: Coverage score calculated, all proposed values follow naming convention, no duplicate testids per page
  * GAP_ANALYSIS.md: Coverage map shows all modules, missing tests have IDs and priorities, broken tests have failure reasons
  * QA_AUDIT_REPORT.md: All 6 dimensions scored, weights sum to 100%, overall score matches weighted calculation

**LAYER 5: GIT WORKFLOW (NEW)**

15. **## Git Workflow** -- Add after Verification Commands.

Content:
- Branch naming: `qa/auto-{project}-{date}` (e.g., qa/auto-shopflow-2026-03-18)
- Commit message format: `qa({agent}): {description}` (e.g., `qa(scanner): produce SCAN_MANIFEST.md for shopflow`)
- Commit conventions: one commit per agent stage, descriptive messages, never commit .env files
- PR template reference: PR description must include analysis summary, test counts by pyramid level, coverage metrics, validation pass/fail status

**LAYER 6: TEAM SETTINGS (NEW)**

16. **## Team Settings** -- Add after Git Workflow.

Content:
- Max concurrent agents: agents in the same pipeline stage can run in parallel (e.g., testid-injector and analyzer can run simultaneously if scan is complete)
- Worktree isolation: each agent operates on the same branch, no worktree splits needed for this system
- Dependency ordering: respect stage transitions from Agent Pipeline section. An agent cannot start until its required inputs exist.
- Auto-advance mode: pipeline stages advance automatically when the previous stage completes and its artifacts are verified

**LAYER 7: AGENT COORDINATION RULES (NEW)**

17. **## Agent Coordination** -- Add after Team Settings.

Content:
- Read-before-write rules (per agent, what it MUST read before producing output):
  * qa-scanner: must read package.json, folder tree, and all source file extensions before producing SCAN_MANIFEST
  * qa-analyzer: must read SCAN_MANIFEST.md + CLAUDE.md standards before producing QA_ANALYSIS, TEST_INVENTORY
  * qa-executor: must read TEST_INVENTORY.md + CLAUDE.md (all POM rules, locator hierarchy, assertion rules, naming conventions) before writing any test file
  * qa-validator: must read CLAUDE.md quality gates + the test files before validating
  * qa-testid-injector: must read SCAN_MANIFEST.md + CLAUDE.md data-testid convention before auditing/injecting
  * qa-bug-detective: must read test execution output + the test source files + CLAUDE.md before classifying

- Handoff patterns: each agent writes its output artifact(s), then the pipeline advances. The next agent reads the artifact(s) as its first action.
- Quality gates per artifact: every artifact must pass its verification command (from Layer 4) before the pipeline advances.

18. **## data-testid Convention** -- Add after Agent Coordination.

Content:
- Naming pattern: `{context}-{description}-{element-type}`
- Context derivation: page-level (from filename or route), component-level (from component name), nested (parent-child, max 3 levels)
- Element type suffix table (the full table from qa-testid-injector SKILL.md: btn, input, select, textarea, link, form, img, table, row, modal, container, list, item, dropdown, tab, checkbox, radio, toggle, badge, alert)
- Dynamic list items: use template literals with unique keys
- Third-party components: props passthrough > wrapper div > inputProps

This file MUST be comprehensive and self-contained. An agent reading ONLY this file should understand all quality standards, its role in the pipeline, what it must read before producing output, what template to follow, and how to verify its output.
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const c=f.readFileSync('CLAUDE.md','utf8');const checks=['Framework Detection','Testing Pyramid','Locator Strategy','Page Object Model Rules','Test Spec Rules','Naming Conventions','Repo Structure','Test Data Rules','Analysis Documents','Quality Gates','Agent Pipeline','Module Boundaries','Verification Commands','Git Workflow','Team Settings','Agent Coordination','data-testid Convention','templates/scan-manifest.md','templates/qa-analysis.md','templates/validation-report.md','qa-scanner','qa-analyzer','qa-executor','qa-validator','qa-testid-injector','qa-bug-detective','qa-planner'];checks.forEach(s=>{if(!c.includes(s))throw new Error('Missing: '+s)});const lines=c.split('\n').length;if(lines<400)throw new Error('Only '+lines+' lines, need 400+');console.log('PASS: '+lines+' lines, all sections present')"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md exists at project root (NOT inside qa-agent-gsd/)
    - Contains ALL 10 existing QA standards sections preserved from qa-agent-gsd/CLAUDE.md: Framework Detection, Testing Pyramid, Locator Strategy, Page Object Model Rules, Test Spec Rules, Naming Conventions, Repo Structure, Test Data Rules, Analysis Documents, Quality Gates
    - Contains Agent Pipeline section with 3 workflow options and stage transition table
    - Contains Module Boundaries table covering all 7 agents with Reads, Produces, Template columns
    - Module Boundaries references all 9 template file paths in templates/ directory
    - Contains Verification Commands section with per-artifact validation rules
    - Contains Git Workflow section with branch naming (qa/auto-{project}-{date}) and commit format (qa({agent}): {description})
    - Contains Team Settings section with concurrent agent rules and auto-advance mode
    - Contains Agent Coordination section with read-before-write rules for all agents
    - Contains data-testid Convention section with naming pattern and element type suffix table
    - File is 400+ lines
  </acceptance_criteria>
  <done>CLAUDE.md at project root contains all 10 existing QA standards plus 7 new sections (Agent Pipeline, Module Boundaries, Verification Commands, Git Workflow, Team Settings, Agent Coordination, data-testid Convention). All 7 agents mapped. All 9 templates referenced. 400+ lines.</done>
</task>

</tasks>

<verification>
CLAUDE.md exists at project root with:
1. All 10 existing QA standards preserved verbatim from qa-agent-gsd/CLAUDE.md
2. Agent Pipeline section with stage transition conditions
3. Module Boundaries table covering all 7 agents and referencing all 9 templates
4. Verification Commands for all 9 artifact types
5. Git Workflow with branch naming and commit format
6. Team Settings with concurrency and dependency rules
7. Agent Coordination with read-before-write rules per agent
8. data-testid Convention with full element type suffix table
9. Cross-reference: every template in templates/ directory is referenced in Module Boundaries
</verification>

<success_criteria>
- CLAUDE.md exists at project root (400+ lines)
- All 10 existing QA standards sections present and accurate
- All 7 new sections present: Agent Pipeline, Module Boundaries, Verification Commands, Git Workflow, Team Settings, Agent Coordination, data-testid Convention
- Module Boundaries references all 9 template files from plans 01-03
- All 7 agent types appear in Module Boundaries with correct Reads/Produces/Template
- Verification Commands cover all 9 artifact types
- Agent Coordination has read-before-write rules for all agents
</success_criteria>

<output>
After completion, create `.planning/phases/02-qa-standards-and-templates/02-04-SUMMARY.md`
</output>
